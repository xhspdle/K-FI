<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kfi.mybatis.ysy.AdminBoardMapper">
	<sql id="search">
		<if test="field != null and field != ''">
			where ${field} like '%'||#{keyword}||'%'
		</if>
	</sql>
	<select id="abmaxcnt" resultType="int">
		select nvl(max(ab_num),1) maxnum from admin_board
	</select>
	<select id="abgetcnt" resultType="int" parameterType="hashmap">
		select nvl(count(*),0) getcnt from admin_board
		<include refid="search"/>
	</select>
	<select id="ablist" parameterType="hashmap" resultType="com.kfi.ysy.admin.vo.AdminBoardVo">
		select * from
		(
			select ablist.*,rownum rnum from 
			(
			select * from admin_board 
			<include refid="search"/>
			order by ab_num desc 
			)ablist
		)
		<![CDATA[ where rnum>=#{startrow} and rnum<=#{endrow}]]>
	</select>
	<select id="abdetail" resultType="com.kfi.ysy.admin.vo.AdminBoardVo">
		select * from admin_board where ab_num=#{ab_num}
	</select>
	<insert id="abinsert" parameterType="com.kfi.ysy.admin.vo.AdminBoardVo">
		insert into admin_board values(#{ab_num},#{admin_num},0,#{ab_content},#{ab_title},sysdate)
	</insert>
	<delete id="abdelete" parameterType="int">
		delete from admin_board where ab_num=#{ab_num}
	</delete>
</mapper>